import type { DefaultTheme } from "styled-components";

  const theme: DefaultTheme = {
    colors: {
      bg: "#fff",
      text: "#000",
      primary: "#B648A0",
      secondary: "#363135"
    },
  };

  export default theme;
